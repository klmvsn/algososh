import { ElementStates } from "./element-states";

export type TNewArray = {
    item: number;
    state: ElementStates
}

export type TInProgress = {
    ascending: boolean,
    descending: boolean
}
